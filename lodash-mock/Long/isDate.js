/*
	_.isDate(value)
	  - 检查 value 是否是 Date 对象。

	参数
	  - value (*): 要检查的值。

	返回
	  - (boolean): 如果 value 是一个日期对象，那么返回 true，否则返回 false。

	例子
		_.isDate(new Date);
		// => true

		_.isDate('Mon April 23 2012');
		// => false
*/
