/*
	_.isMap(value)
	  - 检查 value 是否为一个 Map 对象。

	参数
	  - value (*): 要检查的值。

	返回
	  - (boolean): 如果 value 是一个 Map 对象，那么返回 true，否则返回 false。

	例子
		_.isMap(new Map);
		// => true

		_.isMap(new WeakMap);
		// => false
*/
