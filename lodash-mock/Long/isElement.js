/*
	_.isElement(value)
	  - 检查 value 是否是可能是 DOM 元素。

	参数
	  - value (*): 要检查的值。

	返回
	  - (boolean): 如果 value 是一个DOM元素，那么返回 true，否则返回 false。

	例子
		_.isElement(document.body);
		// => true

		_.isElement('<body>');
		// => false
*/
