/*
	_.isNil(value)
	  - 检查 value 是否是 null 或者 undefined。

	参数
	  - value (*): 要检查的值。

	返回
	  - (boolean): 如果 value 为null 或 undefined，那么返回 true，否则返回 false。

	例子
		_.isNil(null);
		// => true

		_.isNil(void 0);
		// => true

		_.isNil(NaN);
		// => false
*/
