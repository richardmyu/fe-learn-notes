/*
	_.isFunction(value)
	  - 检查 value 是否是 Function 对象。

	参数
	  - value (*): 要检查的值

	返回
	  - (boolean): 如果 value 是一个函数，那么返回 true，否则返回 false。

	例子
		_.isFunction(_);
		// => true

		_.isFunction(/abc/);
		// => false
*/
