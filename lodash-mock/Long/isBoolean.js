/*
	_.isBoolean(value)
	  - 检查 value 是否是原始 boolean 类型或者对象。

	参数
	  - value (*): 要检查的值。

	返回
	  - (boolean): 如果 value 是一个布尔值，那么返回 true，否则返回 false。

	例子
		_.isBoolean(false);
		// => true

		_.isBoolean(null);
		// => false
*/
