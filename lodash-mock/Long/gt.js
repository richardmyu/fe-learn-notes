/*
	_.gt(value, other)
	  - 检查 value是否大于 other。

	参数
	  - value (*): 要比较的值。
	  - other (*): 另一个要比较的值。

	返回
	  - (boolean): 如果value 大于 other 返回 true，否则返回 false。

	例子
		_.gt(3, 1);
		// => true

		_.gt(3, 3);
		// => false

		_.gt(1, 3);
		// => false
*/
