/*
	_.shuffle(collection)
	  - 创建一个被打乱值的集合。 使用 Fisher-Yates shuffle 版本。

	参数
	  - collection (Array|Object): 要打乱的集合。

	返回
	  - (Array): 返回打乱的新数组。

	例子
		_.shuffle([1, 2, 3, 4]);
		// => [4, 1, 3, 2]
*/
