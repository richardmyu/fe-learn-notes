/*
	_.fromPairs(pairs)
	  - 与 _.toPairs正好相反；这个方法返回一个由键值对pairs构成的对象。

	参数
	  - pairs (Array): 键值对pairs。

	返回值
	  - (Object): 返回一个新对象。

	例子
		_.fromPairs([['fred', 30], ['barney', 40]]);
		// => { 'fred': 30, 'barney': 40 }
*/
